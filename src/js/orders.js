// Order lookup/history logic placeholder.
// This module will read real order data once guest checkout and order
// tracking are built in a later milestone. No fake orders are created.

export function getOrderById(orderId) {
  return null;
}

export function getOrderHistory() {
  return [];
}
