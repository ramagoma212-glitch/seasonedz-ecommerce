// Cart line-item component placeholder.
// Will render a single cart row (image, name, quantity controls, subtotal)
// once the shopping cart page is built in a later milestone.

export function renderCartItem(item) {
  return "";
}
