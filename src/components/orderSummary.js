// Order summary component placeholder.
// Will render the items/subtotal/shipping/total breakdown on the
// checkout and order confirmation pages once those are built.

export function renderOrderSummary(order) {
  return "";
}
